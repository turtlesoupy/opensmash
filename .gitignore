BattleShip/
opensmash/
.DS_Store
.env
web-prototype/visual/assets/generated/
web-prototype/.baked-characters/
__pycache__/
*.pyc
.cache/
eval/cells/
eval/ratings.jsonl
eval/state*.json
eval/*.log
eval/pairs.json
eval/*/cells/*/shots/
eval/*/cells/*/shots-full/
# scripts/batch_characters.py working state (results/failures jsonl, logs,
# per-run scratch). The 1000-fighter run's sweep outputs are kept under
# artifacts/batch-1000/.
batch-state/
play/ui/*/*.log
# Generated fighter workspace. Production runtime files are published to GCS
# and pinned by web-prototype/config/baked-assets.json.
play/

# The engine checkout is a sibling symlink here (pipeline/BattleShip -> ../BattleShip).
BattleShip
# Session scratch and one-off local artifacts at the repo root.
scratchpad/
/eval-tour-*.rpl
/eval-tour-*.rpl.json
/hand-cursor-meshy*
/queen-postsmooth.osb
# Reference photos of real people used for local fixtures; never commit them.
refs/
# Generated evaluation output (cells, engine builds, per-experiment sweeps).
eval/*/cells/
eval/*/build/
eval/*/buildworker*/
eval/announcer_*/
eval/canonfinal/
eval/canonll/
eval/canonsamus/
eval/freeze/
eval/gradedarm/
eval/overnight/
eval/pose_check/
eval/samus_check/
eval/shipcheck/
eval/skirtclaim/
eval/smoothing/
eval/streams/
eval/tail_attach/
eval/yawcal/
eval/*/judge_batch_*.json
eval/*/judge_prompt_*.txt
eval/*/*.log
eval/*/state*.json
# Local experiment workspaces (artifacts/experiments stays tracked).
artifacts/accessory-stow/
artifacts/item-mvp/
artifacts/portrait-reference-ab/
artifacts/portrait-style-study/
artifacts/trailer-captures/

# Experiment meshes/bundles under artifacts/experiments are local-only (were LFS).
artifacts/experiments/*.glb
artifacts/experiments/*.osb

# Claude Code per-machine launch config.
.claude/

# Editorial roster lists and the seed-roster working files: local, not
# published. tools/build_wikipedia_people_seed.py reads the config/ lists when
# present and otherwise runs on pure ranking.
config/wikipedia-roster-inclusions.txt
config/wikipedia-roster-exclusions.txt
artifacts/seed-roster/

# Game-derived inputs (sprites, skeletons, part geometry, announcer
# conditioning clips). Rebuilt from your own ROM by tools/derive_from_rom.py
# and tools/derive_skeletons.py; never committed. See README "Game-derived inputs".
skels/*.skel
skels/reference/
skels/*-all.log
skels/*-parts.log
skels/parts/
web-prototype/asset-sources/css-font/portraits/
web-prototype/asset-sources/css-font/locked/
web-prototype/visual/assets/ui_refs/
web-prototype/asset-sources/stone-tile/source-*
eval/announcer_conditioning_corrected/

# Local-only experiment outputs and source captures purged from history.
eval/round2/
web-prototype/asset-sources/n64-console-sketchfab-recovered.glb

# ROM and native outputs from the optional build.py targets.
/build/
